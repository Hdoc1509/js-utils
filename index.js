const { sum, avg } = require('./utils/arith');
const {
  arrayOfNumbers,
  arrayOfStrings,
  sort,
  randomElement,
} = require('./utils/array');

exports.equation = require('./utils/equation');
module.exports = {
  sum,
  avg,
  arrayOfNumbers,
  arrayOfStrings,
  sort,
  randomElement,
};
